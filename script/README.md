## main setup
```bash
$ ./script/setup.sh
```
## font local-install
```
$ ./script/font_install.sh
```

## vim local-install
- install asdf or pyenv, and python dependencies.
- install gettext (for localization)
- use `./vimbuild.sh`
